algoritmo "semnome"

var
   list_num: vetor[1..15] de inteiro
   i, numero: inteiro
inicio
      para i <- 1 ate 15 faca
           list_num[i] <- randi(15) + 1
      fimpara
      
      escreval("Digite o valor que deseja encontrar: ")
      leia(numero)
      
      while

fimalgoritmo
