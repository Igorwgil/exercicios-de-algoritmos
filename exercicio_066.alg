algoritmo "s"
var
    i, n:inteiro
inicio
    escreva("Digite a tabuada que deseja apreder: ")
    leia(n)

    para i <- 1 ate 10 faca
        escreval(n, " x ", i , " = ", n * i)
    fimpara
fimalgoritmo
