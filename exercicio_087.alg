algoritmo "semnome"
var
    f: caracter

procedimento gerador(c: caracter)
inicio

escreval("+-------===========--------+")
escreval(c)
escreval("+-------===========--------+")
fimprocedimento
inicio
       escreval("Digite uma frase: ")
       leia(f)
       
       gerador(f)
fimalgoritmo
